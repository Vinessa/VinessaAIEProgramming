// fleetest.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include <time.h>

void flee();
	
cout <<"You attempt to run away to escape danger!" << endl;
	float rand;
		srand (time(null));
		rand()%100;
		if (rand > 50);
		{
			cout << "You trip over your own feet and fall to the ground. Your clumbsiness has foiled your escape." << endl;
		}


int _tmain(int argc, _TCHAR* argv[])
{
	flee();

	return 0;
}

