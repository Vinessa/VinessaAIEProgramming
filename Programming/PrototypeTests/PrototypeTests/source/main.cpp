//////////////////////////////////////////////////////////////////////////
#include "AIE.h"
#include <string>
#include <cmath>
#include <time.h>
#include <assert.h>
#include <crtdbg.h>
#include <iostream>
#include "PtypeTests.h"
using std::cout;
using std::cin;
using std::endl;
using std::get;

int main()
{
	/*time_t StartTime; // Saves a place to store the the Current time as start time
	time_t CurrentTime; // saves a place to temporarily store the currenttime

	time(&CurrentTime); // gets the current time and assigns it to CurrentTime

	 StartTime = CurrentTime; // says that Start Time has the same value as timer

	cout << StartTime << endl; // prints start time*/

	timer splash
	

	return 0;
}